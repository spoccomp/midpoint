
<h1>My Favorites</h1>

<table class="table table-hover table-striped">
  <thead>
    <tr class="tb-header">
      <th>
        #
      </th>
      <th>
        Location
      </th>
      <th>
        Address
      </th>
      <th>
        Delete
      </th>
    </tr>
  <thead>

  <%# <% @favorites.each do |f| %> %>
  <%# <tr id="favorite <%= f.id %>"> %>
    <td>
      <%# <%= f.id %> %>
    </td>
    <td>
      <%# <%= f.name %> %>
    </td>
    <td>
      <%# <%= f.address %> %>
    </td>
    <td>
      <%# <span><%= link_to '', favorite_path(favorite), remote: true, method: :delete, class: 'glyphicon glyphicon-trash index-icons', id: "#{course.id}", data: { confirm: 'Are you sure?' } %></span> %>
    </td>
  </tr>
  <%# <% end %> %>
</table>
