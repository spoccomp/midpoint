 <%= render 'shared/nav' %>
 
<h3 class="text-center">Email</h3>


<%= bootstrap_form_with(model: @user) do |f| %>
  <%= f.email_field :email %>

  <%= f.submit "Send", class: "col-md-6 offset-md-3" %>
 <% end %>

<%# <%= form_tag({controller: "email_models", action: "create"}, method: "post") do%>
<%# Email : <br><%= text_field_tag 'email', ''%>
<%# subject: <br><%= text_field_tag 'subject', ''%>
<%# Body:<%= text_area_tag 'body', ''%> 
<%# <%= submit_tag "submit",:value => "submit"%> 
<%# <%end%>

